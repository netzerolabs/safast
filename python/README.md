# safast-optical Python package

Camera CLI and wire-compatible decoder for the SAFAST animated QR optical
transfer protocol. See the repository root README for architecture, security
model, and end-to-end usage.
